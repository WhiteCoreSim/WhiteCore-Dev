language: csharp
mono:
  - latest
script:
  - ./runprebuild.sh -c Release -a x86 -b Yes
notifications:
  irc:
    channels:
      - "chat.freenode.net#whitecore-dev"
    on_success: always
    on_failure: always
    use_notice: true
    skip_join: true
